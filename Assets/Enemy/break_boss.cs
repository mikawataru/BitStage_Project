using UnityEngine;
using System.Collections;

public class break_boss : MonoBehaviour {
	void Start () {
		Destroy (gameObject, 0.5f);
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
